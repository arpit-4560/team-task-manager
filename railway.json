{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build",
    "watchPatterns": ["src/**"]
  },
  "deploy": {
    "startCommand": "npx serve dist",
    "restartPolicyType": "ON_FAILURE"
  }
}